# 기여 안내

PR 환영합니다. `main` 에는 직접 푸시할 수 없고, 포크 → PR 로만 받습니다.

## 개발 환경

윈도우 전용입니다. Oodle 바인딩이 `windows.NewLazyDLL` 기반이라 다른 OS로
가려면 `internal/oodle` 에 빌드 태그를 나눠야 합니다.

필요한 것: Go 1.23+, Node 20+, MinGW-w64 g++, [Wails](https://wails.io) v2

```bash
bash scripts/setup.sh   # ooz 소스 클론 + 네이티브 코덱 빌드
bash build.sh           # 앱 + CLI 빌드
```

## 저장소에 넣지 않는 것

이건 취향이 아니라 규칙입니다.

| 넣지 않는 것 | 이유 |
|---|---|
| `assets/icons/` 의 이미지 | 게임 회사 저작물. `scripts/fetch-icons.sh` 로 각자 받습니다 |
| `.sav` 파일 | **다른 사람의 스팀 ID와 캐릭터 정보**가 들어 있습니다 |
| `oo2core*.dll` | RAD/Epic 독점 코덱 |

`.gitignore` 가 막고 있지만, 실수로 우회하지 마세요.
자세한 근거는 [`docs/THIRD_PARTY.md`](docs/THIRD_PARTY.md) 에 있습니다.

## 테스트

```bash
go test ./...
```

**대부분의 테스트는 실제 세이브 파일이 있어야 돌아갑니다.** 픽스처는 위 이유로
저장소에 없어서, 없으면 조용히 skip 됩니다. 초록불이 떴다고 검증됐다는 뜻이
아닙니다 — 세이브를 건드리는 변경이면 본인 세이브로 확인하고, 무엇으로
확인했는지 PR에 적어주세요.

`testdata/` 에 `Level.sav` 와 `Players/` 를 넣으면 전체가 돌아갑니다.

## 이 프로젝트가 검증을 대하는 방식

세이브 편집기는 **조용히 틀리기 쉽습니다.** 여기서 실제로 겪은 것들:

- 파일을 다시 읽어 값을 확인하는 건 검증이 아닙니다. 내가 쓴 걸 그대로 읽는
  것뿐이고, 게임이 받아들일지는 알 수 없습니다. 게임은 유효하지 않은 항목을
  조용히 버립니다.
- 개수를 세는 것으로는 부족합니다. 아이콘 매핑이 "해석되는 개수"로는 통과하면서
  화면에는 엉뚱한 그림을 그리고 있었습니다.
- 팰을 추가할 때 슬롯 번호를 잘못 쓰면 **원래 있던 팰이 지워집니다.** 개수가
  아니라 집합을 비교해야 잡힙니다.

그래서 새 기능에는 **틀렸을 때 실패하는 테스트**를 같이 주세요. 가능하면
일부러 망가뜨려 그 테스트가 정말 실패하는지 확인해보시고요.

## 커밋과 PR

- 커밋 메시지는 **무엇을 왜** 바꿨는지. 특히 알아내기 어려웠던 것(오프셋,
  포맷의 함정)은 다음 사람이 다시 헤매지 않게 적어주세요.
- 한 PR은 한 가지만.
- `go test ./...` 와 `gofmt` 통과.
- 프론트엔드를 건드렸으면 `cd frontend && npx tsc --noEmit`.

## 라이선스

GPL-3.0 입니다. 기여하시면 같은 라이선스로 배포되는 데 동의하는 것으로
봅니다.
